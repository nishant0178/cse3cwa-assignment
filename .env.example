# ============================================
# Environment Variables Template
# ============================================
# Copy this file to .env and update with your actual values
# Command: cp .env.example .env

# ============================================
# DATABASE CONFIGURATION
# ============================================

# PostgreSQL Connection URL
# Format: postgresql://username:password@host:port/database?schema=public
# For Docker: Use 'postgres' as host (service name)
# For local: Use 'localhost' as host
DATABASE_URL="postgresql://escaperoom_user:YourSecurePassword123@postgres:5432/escaperoom_db?schema=public"

# PostgreSQL Credentials
POSTGRES_USER=escaperoom_user
POSTGRES_PASSWORD=YourSecurePassword123
POSTGRES_DB=escaperoom_db

# ============================================
# PGADMIN CONFIGURATION (Optional)
# ============================================

# pgAdmin Web UI Credentials
# Access at: http://localhost:5050
# Start with: docker-compose --profile tools up -d
PGADMIN_EMAIL=admin@escaperoom.local
PGADMIN_PASSWORD=admin123

# ============================================
# NEXT.JS CONFIGURATION
# ============================================

# Node Environment
# Values: development | production | test
NODE_ENV=production

# API URL for client-side requests
# Development: http://localhost:3000/api/scores
# Production: /api/scores (relative path) or https://yourdomain.com/api/scores
NEXT_PUBLIC_API_URL=/api/scores

# ============================================
# PRODUCTION SETTINGS (EC2 Deployment)
# ============================================

# Uncomment and update these for production deployment

# Database URL (if using external DB like AWS RDS)
# DATABASE_URL="postgresql://username:password@your-rds-endpoint.region.rds.amazonaws.com:5432/escaperoom_db"

# API URL (if using custom domain)
# NEXT_PUBLIC_API_URL=https://yourdomain.com/api/scores

# Application Port (default: 3000)
# PORT=3000

# ============================================
# DOCKER COMPOSE OVERRIDES
# ============================================

# Compose Project Name (optional)
# COMPOSE_PROJECT_NAME=escaperoom

# ============================================
# OPTIONAL: GITHUB ACTIONS SECRETS
# ============================================

# These should be added to GitHub repository secrets
# Path: https://github.com/username/repo/settings/secrets/actions

# EC2_SSH_KEY - Your EC2 private SSH key
# EC2_HOST - EC2 public IP or domain (e.g., ec2-44-202-231-206.compute-1.amazonaws.com)
# EC2_USER - SSH username (ec2-user for Amazon Linux, ubuntu for Ubuntu)
# DOCKER_USERNAME - Docker Hub username
# DOCKER_PASSWORD - Docker Hub access token
# DATABASE_URL - Production database connection string

# ============================================
# SECURITY NOTES
# ============================================

# 1. NEVER commit .env file to version control
# 2. Use strong passwords for production
# 3. Rotate credentials regularly
# 4. Use different credentials for dev/staging/production
# 5. Keep .env.example updated (without sensitive values)
